/*
Ejercicio 5.3 del libro Eloquent Javascript (En progreso)
*/

/*function every(array, test) {  
 for(let element of array){
  if(!test(element)){
   return false; 
  }
 }
  return true; 
}*/

function antitest(test){
   return test => test==false; 
  }

function every(array, test){
  
  return array.some(antitest(test));
  
}

